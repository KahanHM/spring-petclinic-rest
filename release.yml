#this release yml intialized details of version of application and docker image tag to use
#this is a must one because based on this image build and push to docker hub


version: "1.0.0"
description: "🐳 Docker image refactor + Trivy security scan"
docker_repo: "kahanhm/spring-petclinic"
apply_changes: true